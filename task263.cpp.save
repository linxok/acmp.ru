#include <iostream>
#include <cstdlib>
#include <cstring>
#include <cmath>

using namespace std;
int main()
{
	int a, b,  n, i, j;
	cin >> n >> i >> j;
	a = abs ( j - i ) - 1;
	b = n - a - 2;
	if ( a > b ) { cout << b; }
	else { cout << a; }

	return 0;
}
